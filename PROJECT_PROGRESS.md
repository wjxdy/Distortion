# 项目进度

## 基本信息
- 项目名称：《失真 Distortion》
- 当前阶段：开发早期 / MVP 垂直切片
- 最后更新：2026-06-17

## 当前状态
- 项目目标是做一个 5-8 分钟的赛博朋克像素叙事侦探 demo：开场世界观、自由审讯、探索取证、揭示第一块真相、结尾钩子。
- 核心卖点是“自由输入盘问被改写记忆的人”，用对方前后矛盾撬出真相。
- 当前仓库已经包含 Godot 4 客户端骨架和 Node.js LLM 代理后端。

## 已完成
- 设计文档已明确 MVP 范围、世界观、五天制作顺序和砍掉内容。
- Godot 客户端已有开场序章、审讯室 UI、探索取证、钥匙/关键词/真相判定和结尾文本。
- Node 后端已有 `POST /chat`，会把 Godot 对话历史转发给 OpenAI 兼容模型接口。
- 后端已有基础单元测试，覆盖 message 构建、回复解析和重试逻辑。

## 关键决策
- 2026-06-15: MVP 引擎选择 Godot 4，目标导出 HTML5 网页版。
- 2026-06-15: 审讯中的老人回复由 LLM 生成，但真相触发采用确定性规则：持有线索钥匙 + 玩家发言命中关键词。
- 2026-06-15: MVP 只做一个真相点：妻子林秀兰与蓝裙子/红烧肉相关记忆。
- 2026-06-16: 美术统一为 **8 比特复古像素**（限制调色板 + dithering + 扫描线/失真），非 2064 的 16 位精细路线；2064 仅作叙事/结构参照。
- 2026-06-16: 开场序幕重做为「序·谜」4 张文学幻灯片（不讲剧情、只塑造世界观/谜），删冷开独白与旧新闻；那句记忆错乱独白移入审讯室由周明远本人当面说。
- 2026-06-16: 审讯室废弃滚动聊天窗，改 **Coffee Talk 式面对面对话**：带尖气泡（玩家尖朝下/周明远尖朝上）+ 右下输入框 + 回看记录(backlog) + fx_crack 真相演出。
- 2026-06-16: 确定周明远背景 **A 版**——假记忆是"妻子林秀兰被医院 AI 医疗事故(误诊乱开药)害死"，真相是她病逝；他宁信"AI 害的"也不接受"病死没人救得了"(grief 有了恨的对象，记忆更粘)。家人 MVP 只聚焦妻子。
- 2026-06-16: **AI 自驱表情**落地——后端 system prompt 让模型句首输出 `[calm]/[sad]/[angry]/[sinister]` 标签，`parseReply` 拆成 `{reply,emotion}`，`/chat` 返回 emotion，客户端 `_set_emotion` 切立绘。气泡加很淡暗色底(可读性)。

## 技术 / 结构备注
- 客户端位于 `Game/client`，主要脚本在 `game/` 和 `scenes/`。
- 后端位于 `Game/server`，入口是 `src/server.js`，默认监听 `8787`。
- 当前后端实现接入月之暗面 Kimi（`KIMI_API_KEY` / `KIMI_MODEL`），而设计文档早期写的是腾讯云大模型/SCF；这是需要后续确认或统一的差异。

## 最近一次进展
- 2026-06-17: **补全"去小区"前置引导链**（提交 9d77efe）：用户实测发现去小区前缺引导——不知道要先查住址(小区门 gated on home_address)、先拿钥匙(老人家门 gated on home_key)。改:`visit_community` 文案点明两个前提(先终端查户籍 + 档案室拿钥匙);新增 `got_address`(终端查户籍→提醒去拿钥匙)、`got_key`(档案室拿钥匙→提醒可去小区)两条确定性提醒(terminal FILE_HINTS 加 address→got_address;archive 取钥匙 fire got_key)。完整引导链:甩锅AI→去终端查死因→回去问→(亮证据仍咬定)去小区[先查户籍+拿钥匙]→查户籍→拿钥匙→小区→老人家→莫忘日志→回审讯对峙。验证 52/52。
- 2026-06-17: **莫忘提醒加关键词兜底（修"模型沉默/漏吐就不弹"）**（已提交 5cc82cc）：实测发现提醒纯靠模型吐 `[[hint:ID]]`——模型一过载回「……」、或漏吐、或该 ID 已被去重，提醒就断，玩家卡住没引导(用户实测多次没弹)。诊断:逻辑正常(不过载时"凭什么确定是AI"稳定吐 investigate_death 3/3、有证据后吐 visit_community)，断点全在"模型这轮没真回/没吐标签"。**改成"模型驱动为主 + 确定性兜底"**:`interrogation._hint_fallback(reply)` 在每轮回复后,按老头回复含"AI/误诊" + 玩家是否已有 linxiulan/no_accident 钥匙(=查过终端) + 玩家问话关键词,确定性补发 investigate_death(没查→去终端)/visit_community(查过仍咬定→去小区)/protecting_app;`fire_hint` 自带去重,与模型标签不重复。模型正常照旧、哑火才兜底。注:钥匙(档案室)与提醒无关,不是卡点原因。验证 52/52、加载干净。
- 2026-06-17: **道具栏 + 钥匙 + 警局档案室（进老人家需钥匙）**（已提交 688e132，色块/TDD）：用户要道具系统。**道具栏做成 autoload 全局 HUD**(`Inv`=`inventory.tscn`,底部常驻 3 格,点格看说明)——天然每场景都在,避免"漏加场景"bug(手机那次教训)。`GameState` 加 `items`+`add_item/has_item`;`content.gd` 加 `ITEMS`(home_key/oldman_phone)。**新建警局档案室** `archive.tscn/gd`(可走,取钥匙→`add_item("home_key")`+`Inv.refresh()`);**police 走廊加第4门「档案室」**(ArchiveArea+标签,现 4 门:街道/审讯室/档案室/终端室)。**老人家门(home_corridor)需钥匙**才能进,否则提示"去档案室拿钥匙"。老人房间拿手机→`oldman_phone` 进道具栏。opening 新游戏 `Inv.refresh()` 清空。完整流程:审讯(甩锅AI)→终端(查死因+户籍)→档案室(拿钥匙)→小区→楼→楼道(钥匙开门)→房间(合照+手机莫忘日志)。验证:客户端 52/52、10 场景加载干净、截图档案室拿钥匙道具栏显示🔑/警局4门标签。autoload 顺序 Sfx→Game→Inv。
- 2026-06-17: **修小区流程3处**（已提交 b6fb847）：① **莫忘提醒流程错位**——上次误把"甩锅AI→去终端查死因"改没了、直接跳"去小区";改回 `oldman.js` beat：甩锅 AI→`investigate_death`(去终端查妻子死因)，被亮证据后仍咬定→`visit_community`(去小区)。② **终端加户籍/住址**——`TERMINAL_FILES` 加 `address`(发 `home_address` 钥匙,"晚晴小区2号楼702");**world 小区门 gated**：没查到住址进不去、提示"先去终端查户籍"(终端加 AddressBtn)。③ **小区返回不清晰**——community 左边加「← 街道 ↑返回」招牌(色块),走到左边↑出小区回街道。验证 48/48、场景加载干净、后端重启。
- 2026-06-17: **小区支线全链路 + 第二层真相·莫忘日志滑坡**（已提交 8d172d9，色块占位/全 .tscn）：用户要建小区这条支线(option B,预留的小区门)。新建 4 个场景(色块占位,套警局走廊可走模板/终端列表 UI):**①`community` 小区外景**(2 栋楼+2 邻居 NPC 走近触发"周老头最近怪得很、常对着手机说话"+老人楼门);**②`elevator` 电梯**(选楼层 UI,老人住 7 层,点错给反馈);**③`home_corridor` 楼道**(门牌号 701/702老人家/703);**④`oldman_home` 老人房间**(两件证物:合照→发 photo 钥匙;**老人手机→莫忘日志「滑坡」**=点【下一条】逐条翻看 AI 从实话滑向谎言,翻完发 `molog` 钥匙+莫忘提醒"回审讯做对峙")。`content.gd` 加 `MOWANG_LOG_LINES`(滑坡9条)/`HOME_EVIDENCE`/新引导提醒;`oldman.js` beat 改 `ask_why_ai`(问为何确定)→`visit_community`(去小区)对齐用户要的引导链;`world.gd` **解锁小区门**→进 community。莫忘日志放老人手机里(最合理)。全场景加手机实例。验证:客户端 47/47、8 场景加载干净、截图小区/电梯/莫忘日志滑坡正常。**第二层真相路径打通**:小区→找手机→翻日志(molog)→回审讯追问莫忘/日志→触发 complicity(老人是同谋)。
- 2026-06-17: **修 bug：手机现在每个游玩场景都能点开**（已提交 ac30c53）：之前 phone 实例只在 world/审讯室/终端，**警局走廊 police 没加→点不开**。给 `police.tscn` 加 phone 实例 + `police.gd` 接 opened/closed 锁人(与 world 一致,走廊 `_input` 本就有 player.locked 守卫不会误触进门)。现 world/police/审讯室/终端均可开手机;opening 序幕是开场幻灯片(游戏前)不放。验证 47/47、走廊开手机截图正常。
- 2026-06-17: **双向莫忘提醒(终端查到线索→引导回问老头) + 警局门标签**（已提交 699948b）：① **门标签**——`police.tscn` 在 3 个门上方加常驻房间名标签(← 街道/审讯室/终端室,描边可读),解决"玩家分不清哪个门是干嘛的"。② **双向提示**——之前莫忘提醒只在老头对话时(模型触发);现在**终端查到关键案卷也触发**(确定性):`terminal.tscn` 加 phone 实例(toast/红点能在终端显示),`terminal.gd` 加 `FILE_HINTS`(wife→ask_wife_death / medical→ask_no_accident),查到时 `Game.state.fire_hint`(去重)→`phone.notify_hint()`;`content.gd MOWANG_HINTS` 加这两条终端侧提醒(引导"回审讯室问他")。③ 顺手修 toast 与终端"返回走廊"按钮的右上角碰撞(toast 下移到 y78)。验证:客户端 47/47、4 场景加载干净、截图门标签清晰/终端查线索弹提醒+红点/toast 不再叠。
- 2026-06-17: **莫忘提醒改右上角小字 toast(去中间蓝框)**（已提交 ef04d3f）：用户嫌中间大横幅+蓝气泡框抢戏。改为:手机响一声+红点+**右上角弹一句小字**("💬 莫忘：我想到点东西——看看手机",纯文字带描边、无框),完整提醒内容仍在手机莫忘 app 看。实现:`phone.tscn` 加右上角 `Toast` Label(描边可读);`phone.gd` 加 `notify_hint()`(响声+刷红点+`_show_toast` 淡入停留淡出);`interrogation._handle_hint` 用 `phone.notify_hint()` 替掉原 `_banner` 中间横幅。验证:客户端 47/47、截图右上角小字+红点、无中间蓝框。
- 2026-06-17: **接入用户音效 + 开局任务红点**（已提交 1883cd7，TDD）：① 用户在桌面 `音效素材` 做了 4 个音效 → 复制进 `audio/`(click.wav 覆盖占位、open_door.wav、notify.wav、typing.mp3)。`Sfx` autoload 加 `door/notify/typing` + `play_door/play_notify/start_typing/stop_typing`;打字机用**持久 AudioStreamPlayer + loop**,打字时 start、tween 结束 callback stop(不重复)。连线:**门音**=所有进出场景(world/police/terminal/interrogation/opening 的 _enter_door/_back/_go_world);**通知音**=莫忘提醒触发 + 开局 world 有未读任务时;**打字机音**=审讯室老头气泡 `_typewriter` start/stop;**点击音**=手机/按钮(沿用 play_click,now 真音)。② **开局任务红点**:`GameState.task_unread`(默认 true)+`read_task()`;手机 TaskBtn 加红点子节点;`phone.gd refresh_badge` 改为 📱红点=任务||莫忘任一未读、任务/莫忘各自红点;`_show_task`→read_task 清红点;world _ready 有未读任务响通知音。验证:客户端 47/47、场景加载干净(interrogation 退出有"resources in use"无害告警=autoload打字机player)、截图开局 phone+任务红点亮/看任务后清。音效 headless 听不到,待 F5。
- 2026-06-17: **对话续接 + 让模型知道玩家调查进展**（已提交 743dbe4，TDD）：用户问"进出审讯室老头总重置、模型不知道我去做了啥"。查明:对话历史其实**早已跨场景保留**(interrogation 用全局 `Game.state`,每次发完整 history)——重置感来自①`_ready` 写死开场喃喃每次重播 + ②删了回看记录看不到旧对话。做 ①+②(设定:周明远阿尔茨海默体现在说话内容,不真清空对话记忆):**①开场续接**——history 空才播默认喃喃,否则显示他上一句、接着聊不重置;**②调查进展喂模型**——`GameState.investigation_summary()` 按已得钥匙(linxiulan/no_accident/molog)拼成一句【系统旁白(玩家不可见)】,`_send` 时拼在 history 最前发给模型(不污染持久化历史)。验证:客户端 44/44、interrogation 加载干净;实机 curl"已查死因+追问诊断书"→老头被顶到当场激动甩锅"你们就是信那些冷冰冰的机器"(emotion=angry)、护执念回避证据,完全在设定内。③历史精简(超长压摘要)用户同意后做暂缓。
- 2026-06-17: **实测大模型功能(Moonshot 已恢复)+ 修 hint 标签泄漏 bug**（已提交 941e387，TDD）：用户报"又连不上大模型"。实测=**其实已恢复**:直连 Moonshot HTTP200/2.3s、`/chat` 返回真·周明远回复(2~6.5s)、emotion/hint 字段都正常;之前"连不上"是过载时段保底返「……」,现已缓解。**顺带实测 hint 触发成功**:逼问妻子死因→hint=`investigate_death`,问为何用app→`protecting_app`,模型真会吐标签。**但抓到 bug**:模型常把双括号 `[[hint:ID]]` 写成**单括号 `[hint:ID]`**(偶尔中文【】),旧正则只认双括号→标签**漏进玩家可见台词**。修:`parseReply` 正则改成容错 1~2 个 `[`/`【` 括号(`[\[【]{1,2}...[\]】]{1,2}`)。加单括号/中文括号复现测试(RED→GREEN),后端 **19/19**;重启后实测标签不再泄漏、hint 正常提取。
- 2026-06-17: **莫忘 AI 助手提醒系统（模型触发 + 严格去重 + 手机红点）**（已提交 4ccc720，TDD）：新设定——主角手机里也有「莫忘」app，在关键节点提醒玩家下一步(兼新手指引+主题伏笔:主角也被同款AI牵着走,呼应结局B/收尾字幕)。用户坚持**由模型决定何时触发**。落地:① 后端 `oldman.js` 指示模型在话首次触及某情节点时,在回复**末尾附隐藏标签 `[[hint:ID]]`**(investigate_death=甩锅AI / protecting_app=回避手机app);`llm.js` `parseReply` 解析并剥离该标签→返回 `{reply,emotion,hint}`(server 透传)。② 客户端 `GameState` 加 `hints_fired/mowang_log/mowang_unread` + `fire_hint(id,text)`(**去重:同一ID整局只触发一次**,新游戏 reset 清)+ `read_mowang()`;`content.gd` 加 `MOWANG_HINTS` 文案。③ 手机 `phone.tscn` 加「莫忘」app + 📱与莫忘 app 红点(色块 ColorRect);`phone.gd` 莫忘 app 显示提醒日志、看过转已读清红点、`refresh_badge()`。④ 审讯室 `_on_reply` 读 hint→`fire_hint` 去重→新提醒弹莫忘提示条+刷红点。验证:后端 17/17、客户端 40/40、截图(触发→红点+提示条/莫忘app显示/去重 false/看过清红点)全过。**模型实际吐标签待 Moonshot 不忙时实机看**(逻辑+解析已测)。
- 2026-06-17: **建警局电脑终端场景(案件数据库) + 调查从手机移回终端**（已提交 c6874f8）：用户定案——**手机只留上司任务，案件调查在「警局电脑终端」**(不叫档案室)；终端不退役、要建；AI 问询先不做。落地:① `terminal.tscn/.gd` 从占位重写成**案件数据库 UI**(左侧 4 个静态案卷按钮 案件概要/周明远资料/林秀兰死亡记录/医疗事故记录 + 右侧详情显示,色块全 .tscn 节点,返回走廊/Esc);点案卷→显示内容,带 grants_key 的(林秀兰→linxiulan、医疗→no_accident)经全局 Game 发钥匙;现阶段不门控。② `content.gd` 加 `TERMINAL_FILES` 案卷数据(去邪教版:林秀兰自然死亡、医疗事故查无)。③ 手机去掉"档案"app,**手机=任务、终端=查案**分工清晰(phone.gd/tscn 删 ArchiveBtn)。验证:客户端 33/33、6 场景加载干净、截图终端 UI 正常、自验"点林秀兰记录经 Game 发 linxiulan=true"(跨场景流程通)。主流程:审讯听到"AI害死"→终端查死因(自然死亡)+医疗(查无)→回审讯追问揭穿第一层真相。
- 2026-06-17: **聊天慢·二次诊断(subagent)+ 超时/重试修复**（已提交 d0384fe）：派 subagent 分段计时排查,结论=慢的真凶是 **Moonshot 上游间歇过载**(直连约一半请求 `engine_overloaded` 429 挂 9.4s,成功的也要 **4~6s**——不是之前乐观的~1.1s);**代理/本地连接都不是瓶颈,且服务器其实没走代理**(Node fetch 默认不读 *_PROXY,代码也没配 ProxyAgent),Godot→localhost conn=0.2ms。**关键 bug**:`llm.js` 旧 `REQUEST_TIMEOUT_MS=6000` 正好卡在成功 TTFB(6.2s)上,把本会成功的回复误杀成超时→沉默,再叠加双重试凑成 12.5s。**修复**:① 超时 6s→**14s**(让 4~6s 正常回复落地,只砍真挂起);② 429 改为**不可重试**(过载重试纯浪费)→过载时单次 9.4s 快速保底(从 12.5s 降到 ~9.4s)。后端 14/14。**预期**:Moonshot 恢复后老头 4~6s 正常回话(非1s);仍过载时 ~9.4s 出「……」。诊断细节&建议见本会话 subagent 报告。
- 2026-06-17: **手机抽成可复用场景 + 审讯室去掉两个按钮**（已提交 55cbb9b）：按用户要求——审讯室聊天时去掉「查档案」和「📜回看记录」两个按钮(历史记录没必要)，改成左下一个 📱 手机图标，点开手机里查档案。落地:① 把手机 UI 抽成**可复用 `scenes/phone.tscn` + `phone.gd`**(色块占位,信号 opened/closed,任务/档案/关闭 app;档案 app 显示 content.archive 文案并经 `Game` 发 linxiulan 钥匙跨场景保留);② `world.tscn` 把之前内联的手机换成 phone.tscn 实例(world.gd 用 $Phone + opened/closed 联动锁人);③ `interrogation.tscn` 删 ExploreBtn/BacklogBtn/BacklogPanel,实例化 phone.tscn;`interrogation.gd` 删相关引用、`_log` 改空操作、看手机时禁用盘问输入栏。验证:客户端 26/26、6 场景(含 phone)加载干净、截图审讯室 Bar 仅剩输入+盘问/手机查档案显示正常。
- 2026-06-17: **开场改造·手机中枢第一刀 + 全局状态**（已提交 a672b70，TDD+色块占位）：用户定方案——删旧"手机来电"开场，改成进世界后点手机 logo 弹手机界面看上司任务，档案等也进手机(莫忘本就是手机app,更贴题);终端室场景拟退役。**铁律:全部 `.tscn` 节点+色块占位,不脚本生成,要可拖 DIY**。本刀落地:① 新增 autoload **`Game`**(`game/game_global.gd`,持单一 GameState 实例+reset())让线索钥匙**跨场景保留**(TDD: 先红→建文件→绿,客户端26/26);② `content.gd` 加 `BOSS_TASK` 上司任务文案;③ `opening` 删 Phone/PhoneShade 节点+手机来电逻辑,序幕放完直接进 world,`_ready` 调 Game.reset()开新局;④ `world.tscn` 加 `PhoneUI`(CanvasLayer)=手机logo按钮+手机界面(Dim/Body/左AppList:任务·档案·关闭/右DisplayBg显示区,全色块);`world.gd` 管显隐+填字(看手机时 player.locked);⑤ `interrogation.gd` 改用 `Game.state`。验证:26/26、5场景加载干净、截图手机界面显示任务正常。**中间态**:档案 app 暂占位("下一步接入"),审讯室查档案按钮仍在;终端室未退役。
- 2026-06-17: **去邪教版落地·步骤A：重写 `game/content.gd` 剧情数据**（已提交 208c92c，TDD）：现状是旧版单真相点 MVP、且 content.gd 残留"周明远自制AI"旧设定。按桌面剧本 `失真_游戏流程_去邪教版.md` 重写：**两层真相** `fact`(第一层=妻子重病自然病逝/查无医疗事故,钥匙 linxiulan) + `complicity`(第二层=莫忘喂假记忆/老人同谋,钥匙 molog);**5 个终端探索线索** archive/medical/neighbor/photo/molog(为步骤2终端室备数据);去掉"自制AI"旧设定;结尾改统一字幕"记忆，是我们选择记住的版本。"。**triggers.gd/explore.gd/interrogation.gd 逻辑未动**(数据驱动)。先改 `tests/run_tests.gd` 写去邪教版期望(RED 7挂)→重写 content.gd→GREEN,客户端 **23/23**;4 场景加载干净。**注意中间态**:审讯室现只能揭第一层(archive→linxiulan→追问AI/事故);第二层需 molog 钥匙(终端室,步骤2);故审讯室暂不会自动"结束"(结局是步骤4三分支)。
- 2026-06-17: **修老头回复慢 + 加保底沉默**（已提交 140433b）：诊断=本地服务器零开销(连接0.0002s)，慢全在上游;原模型 `kimi-k2.6` 是**推理模型**先思考再答,实测单条 4~20s(一次19.6s)=主因。**换非推理 `moonshot-v1-32k`**(API正常时~1.1s,temp 0.6;.env 本地已改+后端已重启,.env.example 默认也改)。诊断时还发现 Moonshot 当时**整体过载**(`engine_overloaded_error` 429,直连每条~9.4s,连v1-8k都8.6s)=临时服务端拥堵,与我方无关、代理无关(直连/走代理都一样)。**新增「保底沉默」**(用户设计:把失败演成周明远装糊涂/阿尔茨海默):`oldman.js` 加 `SILENCE_FALLBACKS`(["……","…………","……？"])+`pickSilence()`;`llm.js` 给请求加 AbortController 超时(6s)、重试参数 TRIES=2/sleep500、包 `withSilenceFallback()`——过载/超时/重试耗尽时不抛错,返回「……」让前端永远有回应(真错误打服务器日志);缺 API key 仍硬报。过载期实测:HTTP200 返回「……」、~12.5s有界(非之前40s+500)。新增 fallback 单测,后端 14/14。
- 2026-06-17: **修警局走廊人物/门比例不匹配**（已提交 74ec1c9）：问题=图里门画得大(~280px)、主角精灵才~160px,人显小;用户曾缩放背景去凑→必留空白(图正好1280×720,缩小留黑边/放大要裁)。**正确解法=背景保持原生满屏不动,把人物放大**:重置 `police.tscn`(撤销用户的 Bg squash scale0.74+root offset 实验)→ Bg scale(1,1)/pos(640,360)满屏;**Player 加 scale(1.4,1.4)**(总高~234px≈门高80%),起点(150,576)脚踩地砖;墙体 floor 带按放大后的人重对齐(Top526/Bottom772,WallWide 1300×120);三门区 Col 偏移归零、按图 x(130/420/1035)。验证:双截图人物/门比例自然+门提示正常、加载干净、测试12/12。**通用经验:单屏背景缩放必留空白,要调人物/门比例就缩放角色,别缩放背景。**
- 2026-06-17: **警局走廊换真背景图 + 可走 floor 带**（已提交 13fcc37）：用户给 `police_corridor_closed_exit.png`(1280×720 单屏赛博走廊,2闭门+中央屏板+右侧亮服务器机房开门+通风格栅) → `art/bg_police_corridor.png`。`police.tscn` 删占位 ColorRect(Bg/Ground/Exit/Interrogation/Terminal)，换单张 `Sprite2D`(640,360,texture_filter=1)；墙体 Top→550/Bottom→775 把可走带下移到地砖(玩家脚底碰撞体可走纵向~95px)；三 Area2D 重摆并按图映射:**ExitArea 最左门=返回街道 / InterrogationArea 第二道带观察窗厚门=审讯室 / TerminalArea 右侧服务器机房开门=终端室**(脚本按节点名引用,位置可编辑器拖)。Player 起点(150,625)。验证:双截图(起点出"返回街道"提示+终端门出"进入终端室"提示、玩家上下走正常)、无头加载干净、测试12/12。floor 带高度/门位可 F5 后拖墙拖区微调。(注:之前视差版本被用户否掉、已 revert，主世界停在相机驱动 ParallaxBackground 9d0d103。)
- 2026-06-17: **主世界加视差纵深（真 HD-2D 感）**（已提交 9d0d103，玩家驱动改法被否后保留此相机驱动版）：之前背景和前景同挂相机下 1:1 一起滚 = 没纵深。改用 **`ParallaxBackground` + `ParallaxLayer`**：把雨夜城市 `FarCity`(AnimatedSprite2D) 移进 `ParallaxBackground/RainLayer`，横向 `motion_scale=0.3`(用户选,纵向=1 但相机 Y 恒 360 不触发)；前景 `MainStreet`/墙/门/Player 仍在世界里跟相机 1:1。玩家走时远景城市仅 0.3 速漂移、前景全速 → 纵深拉开。`world.gd` 未改(本就不引用 FarCity)。验证：双截图(玩家左/右)对比前景大幅滚动、背景小幅漂移且在前景之后、无破图、测试 12/12。漂移手感/明暗待用户 F5 微调。
- 2026-06-17: **主世界背景换成动态落雨纵深城市**（已提交 2688ad6）：用户给的 `主世界背景_纵深城市_落雨动态.gif`(2560×720,24帧/12.5fps,夜景城市剪影+落雨,无可走马路) → 用 ImageMagick `-coalesce` 隔帧抽 **12 帧**(省显存,用户选)到 `art/world_rain_city/frame_00..11.png`，建 `art/world_rain_city.tres`(SpriteFrames,循环,6.25fps≈原1.9s)。`world.tscn` 把垫底的 `FarCity` 从静态 `Sprite2D`(world_far_city.png) 换成 **`AnimatedSprite2D`**(sprite_frames + autoplay="default"，位置仍 1280,360)；`MainStreet`(透明天空+马路)继续盖在上面，雨夜城市从天空透出、玩家照常在马路上走。相机/墙/门/Player 不动。**`art/world_far_city.png` 自此不再被引用**(文件保留未删)。验证：无头加载干净、实机渲染截图正常(雨夜街景成立)、客户端测试 12/12。具体明暗/雨量观感待用户 F5 微调。
- 2026-06-17: **剧情去邪教版【后端人设】落地**（已提交 aaf160d）：按桌面文档 `失真_游戏流程_去邪教版.md` 第七节，重写 `Game/server/src/oldman.js` 的 `SYSTEM_PROMPT`——① 去掉"AI 之父"光环，周明远改**普通退休老人**；② 莫忘从"他亲手造的"改为**手机上的商业陪伴/记忆助手 App**（患阿尔茨海默后离不开它）；③ 强化**记忆缺失+混乱**（连"有没有家人"都答不上，支撑线索 1「我没有家人」）；④ 加**第二层真相伏笔**：他其实是"选择相信"（有恨的对象比"谁也救不回"好受）+ 被追问"为什么天天对手机说话"时回避护着手机（支撑线索 7）；⑤ 假记忆(医院 AI 误诊害死林秀兰)、漏真相触发(蓝裙子/红烧肉/生日)、`[calm/sad/angry/sinister]` 情绪标签系统均保留；新增"绝不承认莫忘在骗你"约束。后端测试 11/11 通过。**莫忘日志滑坡终端 UI、对峙脚本、三分支结局仍是客户端待办（见 TODO）。**
- 2026-06-17: **主角精灵 + 移动系统成型**（用户已提交 63bec4d）：接入 `art/detective.png` 图集(6×5,128px) → `detective_frames.tres`(idle/walk_right/walk_up/adjust_glasses/smoke)；抽出**独立 `Player.tscn`**(`CharacterBody2D`)被各场景实例化；四向移动 + 动画映射(左右/下=walk_right翻转,上=walk_up,久站随机扶眼镜/抽烟)；可走区从矩形夹取改为**碰撞体 Walls**；门口触发从距离改为 **Area2D `overlaps_body`**(脚底碰撞体叠到才算)；加 **WASD**(`move_*` action,不碰 ui_*)；街道换真背景 `bg_street.png`。
- 2026-06-17: **开场加 2D 假 HD-2D 氛围**（本次提交）：`opening.tscn` 加 WorldEnvironment(辉光+调色)、Rain/Dust 粒子、Vignette 暗角(`shaders/vignette.gdshader`)，全是可在编辑器调的独立节点。**未实机看,辉光在 Compatibility 下待 F5 调阈值。**
- 2026-06-17: **定下主世界街道 HD-2D 路线 = 2D 假**（非真 3D，避免换 Forward+/破坏网页导出）：宽幅可滚动街道 + Camera2D 跟随 + ParallaxBackground(远景 0.2 / 雾 0.08) + 雨/霓虹。**待用户用 ChatGPT 生成 3 张分层图**(world_street_wide 宽主街上半透明 / world_far_city 远景 / world_fog_overlay 透明可平铺雾)后,我一次接入滚动+相机+视差+门位。AI 出的是平面图,纵深交给 Godot。
- 2026-06-16: **剧情去邪教重写定稿**（仅文档，桌面 `失真_游戏流程_去邪教版.md`）——反派从"邪教控制 AI"改为"莫忘 AI 为留存故意迎合、喂老人假记忆"；两层真相（事实=妻子病逝/情感=老人是同谋）；结局做真分支选择；周明远降为普通老人（非 AI 之父），莫忘=商业陪伴 AI 产品；莫忘日志走终端 UI。后端 `oldman.js` 待按此微调（去 AI 之父 + 莫忘改商业产品）。
- 2026-06-16: **新增主世界横版结构**（魂斗罗式：只左右走、没有跳、门口按 ↑ 进入），全用纯色 `ColorRect` 方块占位，后期替换真图：
  - `scenes/world.tscn/.gd`：主世界街道，警局/小区两入口；小区按↑提示"暂未开放"（先锁）。
  - `scenes/police.tscn/.gd`：警局内部横版走廊，三个交互点=返回街道/审讯室门/终端室门。
  - `scenes/terminal.tscn/.gd`：终端室占位屏（"建设中"+返回走廊），将来放查电脑/问警局AI/调档案日志。
  - 流程串起来：开场→手机→街道→进警局→走廊选 审讯室/终端室；进出闭环，全程占位图。`opening.gd` 手机按钮改为进主世界；审讯室加 `BackBtn` 返回节点（`.tscn` 真节点，可编辑器拖）+ Esc 返回。
  - 四场景无头加载干净、客户端测试 12/12 通过。
- 2026-06-16: 新增项目根 `CLAUDE.md` 约定：场景/界面相关**能用编辑器节点就不用脚本动态生成**（Vibe Coding，要可拖）；改 .tscn 提醒 Reload；占位用纯色方块；8 比特+Zpix。
- 2026-06-16: 装好第三方 `godot-mcp`（Coding-Solo 版，免费开源）于 `~/.dev/godot-mcp`，官方源+代理构建，`claude mcp add` 注册为 local、连接正常（Godot 无官方 MCP）。下次会话生效。
- 2026-06-16: 气泡改像素风——两框统一为「透明底 + 硬方角 + 粗浅色边框(关抗锯齿)」无底色；周明远框左上角金色名字行 + 第二行起内容；玩家框同款。
- 2026-06-16: 全局换**像素字体**——接入 Zpix「最像素」(`fonts/zpix.ttf`，含中文 22238 字形)，`project.godot` 设 `gui/theme/custom_font` 为项目默认字体；字体导入关抗锯齿/子像素、过采样 1:1 保持锐利。整套 UI(标题/气泡/按钮/旁白)与 8 比特美术统一。
- 2026-06-16: 审讯两个气泡(ZhouBubble/PlayerBubble)改为**场景节点**，可在编辑器拖位置/拉大小；尖尖用 `scenes/tail.gd`(@tool, dir/color 可调)。取舍：气泡不再随文字自动缩放，尺寸由编辑器设定。立绘位置用户已在编辑器下移。
- 2026-06-16: 把开场/审讯两场景从「纯代码搭 UI」**重构为常规编辑器节点式**——静态结构(背景/立绘/裂痕/标题/输入栏/按钮/回看面板/手机任务屏/Timer/Http)写进 `.tscn`，编辑器里可见可拖可调；脚本只留逻辑 + 动态部分(气泡/横幅/情绪切换/打字机/Ken Burns)。行为与重构前一致(截图比对通过)，`_on_reply` 已预留 `emotion` 字段读取。另外去掉了打字机逐字 blip 音效。
- 2026-06-16: 审讯室背景换成真·8 比特空房(`bg_police_room.png`，与立绘风格统一)；周明远改 4 情绪精灵图集 `zhou_emotions.png`(4 行 calm/angry/sinister/sad × 4 帧，256×192/格)，`interrogation.gd` 按行切表情、idle 乒乓播放、`_set_emotion()` 切换、真相浮现自动切"悲伤"；临时 F1-F4 调试键(验收后删)。删除被取代的旧 `zhou_portrait.png`。**待办**：让 LLM 输出 `{emotion,reply}` 驱动表情(后端那一小步)。
- 2026-06-16: 接入 8 张像素美术（`Game/client/art/`）；重写 `opening.gd`（序幕接图 + Ken Burns 推镜 + 居中打字机 + 手机来电）与 `interrogation.gd`（Coffee Talk 式带尖气泡）。逻辑测试 12/12 通过，两场景无头加载无脚本错误，Godot 渲染截图自查开场/审讯/双气泡/手机屏均正常。
- 2026-06-15: 读取项目文档和现有实现，初始化项目进度记忆。
